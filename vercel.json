{
  "framework": "vite",
  "buildCommand": "npm install --include=dev && chmod +x node_modules/.bin/vite && npx vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install --include=dev"
}
